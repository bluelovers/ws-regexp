{
  "name": "ws-regexp",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/@lazy-cjk/*",
    "packages/@regexp-cjk/*",
    "packages/@node-novel/*",
    "packages/*"
  ],
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "lerna:publish": "yarn run prepublishOnly && npx lerna publish && yarn run postpublishOnly",
    "lerna:publish:yes": "yarn run prepublishOnly && npx lerna publish --yes --cd-version patch && yarn run postpublishOnly",
    "prepublish:fix-links": "npx @yarn-tool/fix-ws-links",
    "prepublish:lockfile": "npx sync-lockfile .",
    "prepublishOnly": "yarn run prepublish:fix-links && yarn-tool ws run --scope cjk-conv prepublishOnly && yarn-tool ws run prepublishOnly:lerna",
    "postpublish:subtree": "ts-node ./script/run/postpublish-subtree.ts",
    "postpublish:subtree:cjk-conv": "git subtree push cjk-conv master --prefix packages/cjk-conv",
    "postpublish:subtree:regexp-cjk": "git subtree push regexp-cjk master --prefix packages/regexp-cjk",
    "ncu": "npx yarn-tool ncu -u && npx yarn-tool ws exec yarn-tool ncu -- -u",
    "postpublishOnly": "yarn run postpublish:subtree",
    "postpublishOnly:add": "ts-node ./script/run/postpublish-add.ts",
    "sort-package-json": "npx yarn-tool sort && npx yarn-tool ws exec yarn-tool sort"
  },
  "resolutions": {},
  "devDependencies": {
    "@bluelovers/tsconfig": "^1.0.19",
    "@git-lazy/spawn": "^1.0.6",
    "@types/jest": "^25.2.3",
    "@types/lodash": "^4.14.155",
    "@types/node": "*",
    "@yarn-tool/find-tsconfig": "^1.0.9",
    "@yarn-tool/fix-ws-links": "^1.0.5",
    "build-ts-file": "^1.0.9",
    "jest": "^26.0.1",
    "ts-jest": "^26.1.0"
  },
  "peerDependencies": {
    "lerna": "^3"
  }
}
